import React from 'react'
import { View, Text } from 'react-native'

const MoviesList = () => {
    return (
        <View>
            <Text>not yet</Text>
        </View>
    )
}

export default MoviesList
